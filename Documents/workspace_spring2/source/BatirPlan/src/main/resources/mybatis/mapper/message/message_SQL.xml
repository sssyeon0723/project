<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.batirplan.message.mapper.MessageMapper">
    <insert id="sendMessage">
        INSERT INTO TB_MESSAGE (MSSAGE_NO, DSPTCHER, RCVER, CN, SNDNG_DT, MESSAGE_STTUS, RECOVRY_POSBL_AT)
        VALUES (MESSAGE_SEQ.NEXTVAL, #{dsptcher}, #{rcver}, #{cn}, SYSDATE, 'UNREAD', 'N')
    </insert>

    <select id="getReceivedMessages" resultType="kr.or.batirplan.message.vo.MessageVO">
        SELECT * FROM TB_MESSAGE WHERE RCVER = #{rcver} ORDER BY SNDNG_DT DESC
    </select>

    <update id="updateMessageStatus">
        UPDATE TB_MESSAGE SET MESSAGE_STTUS = 'READ', RECPTN_DT = SYSDATE WHERE MSSAGE_NO = #{mssageNo}
    </update>
</mapper>

  
  